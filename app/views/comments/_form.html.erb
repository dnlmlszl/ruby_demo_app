<%= form_with(model: [post, comment]) do |f| %>

<div class="form-control">
  <%= f.rich_text_area :body, class: "my-3" %> <%= f.submit submit_label, class:
  "btn btn-primary my-3" %>
</div>
<% end %>
